# Website

Marketing site built with Vite (via vite-plus). Start the dev server with `pnpm dev` from this directory or `pnpm website:dev` from the root directory (runs on `localhost:5173`).

## Testing

You can test the website with Libretto itself. Start the dev server, then use `libretto open` to open the local URL and verify pages render correctly.
